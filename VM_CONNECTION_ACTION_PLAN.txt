╔═══════════════════════════════════════════════════════════════════════════╗
║                                                                           ║
║                  🔧 UBUNTU VM CONNECTION TROUBLESHOOTING                 ║
║                        ACTION PLAN                                        ║
║                                                                           ║
╚═══════════════════════════════════════════════════════════════════════════╝

❌ CURRENT ISSUE
═══════════════════════════════════════════════════════════════════════════
Cannot reach ubuntu@192.168.1.100 (SSH timeout)

Diagnosis: The IP address 192.168.1.100 is not responding to SSH or ping.
This could mean:
  ❌ VM is powered off
  ❌ Wrong IP address
  ❌ VM is not on same network
  ❌ SSH service is not running

═══════════════════════════════════════════════════════════════════════════

🚀 IMMEDIATE ACTION (Choose One)
═══════════════════════════════════════════════════════════════════════════

OPTION 1: AUTOMATED DIAGNOSTIC (Recommended)
────────────────────────────────────────────────────────────────────────────

Run the new diagnostic script I just created:

  $ ./test-vm-connection.sh 192.168.1.100

This will automatically test:
  ✅ Network ping to VM
  ✅ SSH port 22 availability
  ✅ SSH connection
  ✅ System resources

If it finds your VM, you can immediately deploy!

────────────────────────────────────────────────────────────────────────────

OPTION 2: MANUAL INVESTIGATION (Step-by-Step)
────────────────────────────────────────────────────────────────────────────

Step 1: Get your actual VM IP address

  If you can access Ubuntu VM terminal directly:
  ────────────────────────────────────────────
    On Ubuntu VM, run:
    $ hostname -I

    Example output: 192.168.1.105 (this is your real IP!)

  If you can't access VM terminal:
  ────────────────────────────────────
    Check your router's DHCP list:
    1. Open browser → http://192.168.1.1
    2. Find your router login (check router bottom/back)
    3. Look for "Connected Devices" or "DHCP Clients"
    4. Find "ubuntu" in the list
    5. Note its actual IP (e.g., 192.168.1.105)

  Alternative: Network scan
  ─────────────────────────
    Install arp-scan:
    $ brew install arp-scan

    Scan your network:
    $ sudo arp-scan -l 192.168.1.0/24

    Look for entries that look like "ubuntu" or just "Linux"

Step 2: Test SSH to the CORRECT IP

  $ ssh ubuntu@<THE-IP-YOU-FOUND>

  Examples:
    $ ssh ubuntu@192.168.1.105
    $ ssh ubuntu@10.0.0.50

  You might see:
  ──────────────
    "Are you sure you want to continue? (yes/no)" → Type: yes
    "ubuntu@...'s password:" → Enter the VM password

  If this works, you found the right IP! ✅

Step 3: Run deployment with correct IP

  $ ./deploy-to-ubuntu-vm.sh <THE-CORRECT-IP>

  Example:
    $ ./deploy-to-ubuntu-vm.sh 192.168.1.105

  ✅ Deployment will now work!

═══════════════════════════════════════════════════════════════════════════

📋 TROUBLESHOOTING EACH SCENARIO
═══════════════════════════════════════════════════════════════════════════

SCENARIO 1: VM is powered off
─────────────────────────────
What to do:
  1. Power on your Ubuntu VM
  2. Wait 30 seconds for it to boot
  3. Try SSH again: ssh ubuntu@<ip>

Expected time: ~1-2 minutes

────────────────────────────────────────────────────────────────────────────

SCENARIO 2: Wrong IP address
─────────────────────────────
What to do:
  1. Get actual IP from VM terminal: hostname -I
  2. Or check router's DHCP client list
  3. Test SSH to correct IP: ssh ubuntu@<correct-ip>
  4. Once working, deploy: ./deploy-to-ubuntu-vm.sh <correct-ip>

Expected time: ~2-3 minutes

────────────────────────────────────────────────────────────────────────────

SCENARIO 3: SSH service not running on VM
──────────────────────────────────────────
What to do (on Ubuntu VM terminal):
  $ sudo systemctl status ssh

  If it says "inactive":
    $ sudo systemctl start ssh
    $ sudo systemctl enable ssh

  If port 22 is blocked:
    $ sudo ufw allow 22/tcp

Expected time: ~1 minute

────────────────────────────────────────────────────────────────────────────

SCENARIO 4: Network connectivity problem
────────────────────────────────────────
What to do:
  1. Check VM network settings
  2. Verify Mac and VM are on SAME network
  3. Check WiFi/Ethernet connection
  4. Restart network: sudo systemctl restart networking

Expected time: ~2-5 minutes

═══════════════════════════════════════════════════════════════════════════

🎯 SUCCESS CRITERIA
═══════════════════════════════════════════════════════════════════════════

You're ready to deploy when:

✅ $ ssh ubuntu@<your-ip> 'echo connected'
   Returns: "connected"

Or:

✅ $ ping -c 1 <your-ip>
   Returns: "1 packets transmitted, 1 received"

And:

✅ $ nc -zv <your-ip> 22
   Returns: "Connection to <ip> port 22 [tcp/ssh] succeeded!"

═══════════════════════════════════════════════════════════════════════════

📞 HELP COMMANDS
═══════════════════════════════════════════════════════════════════════════

Check what files we created for you:
  $ ls -lh TROUBLESHOOT_VM_CONNECTION.md test-vm-connection.sh

Read full troubleshooting guide:
  $ cat TROUBLESHOOT_VM_CONNECTION.md

Run quick diagnostic:
  $ ./test-vm-connection.sh <your-ip>

Verbose SSH test:
  $ ssh -vvv ubuntu@<your-ip>

Network scan (requires arp-scan):
  $ brew install arp-scan
  $ sudo arp-scan -l 192.168.1.0/24

═══════════════════════════════════════════════════════════════════════════

🎯 NEXT STEPS
═══════════════════════════════════════════════════════════════════════════

1. Try automated diagnostic:
   $ ./test-vm-connection.sh 192.168.1.100

   OR find correct IP by running on Ubuntu VM:
   $ hostname -I

2. Update deployment command with correct IP:
   $ ./deploy-to-ubuntu-vm.sh <YOUR-ACTUAL-IP>

3. Deployment will then proceed automatically! ✅

═══════════════════════════════════════════════════════════════════════════

📚 DETAILED GUIDES CREATED
═══════════════════════════════════════════════════════════════════════════

1. TROUBLESHOOT_VM_CONNECTION.md (12KB)
   └─ Comprehensive troubleshooting guide
      • Issue diagnosis
      • Step-by-step solutions
      • Common scenarios
      • SSH key troubleshooting

2. test-vm-connection.sh (executable)
   └─ Automated diagnostic script
      • Tests ping
      • Tests SSH port
      • Tests SSH connection
      • Checks system resources

3. VM_CONNECTION_ACTION_PLAN.txt (this file)
   └─ Quick reference action plan
      • What to do immediately
      • Troubleshooting by scenario
      • Success criteria
      • Help commands

═══════════════════════════════════════════════════════════════════════════

Remember: Once you can SSH to your VM, deployment is automatic! 🚀

